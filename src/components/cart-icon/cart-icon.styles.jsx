import styled from '@emotion/styled';

export const CartIconStyles = styled.div`
  cursor: pointer;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;

  span {
    position: absolute;
    margin-bottom: -8px;
  }
`;
